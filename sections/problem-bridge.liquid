{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .color-scheme-{{ section.id }}.color-custom {
    --color-background: {{ section.settings.custom_colors_background.red }}, {{ section.settings.custom_colors_background.green }}, {{ section.settings.custom_colors_background.blue }};
    --gradient-background: {% if section.settings.custom_gradient_background != blank %}{{ section.settings.custom_gradient_background }}{% else %}{{ section.settings.custom_colors_background }}{% endif %};
    --color-foreground: {{ section.settings.custom_colors_text.red }}, {{ section.settings.custom_colors_text.green }}, {{ section.settings.custom_colors_text.blue }};
  }
{%- endstyle -%}

<div class="problem-bridge color-scheme-{{ section.id }} color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="problem-bridge__container problem-bridge__container--{{ section.settings.text_alignment }}">
      {% if section.settings.heading != blank %}
        <h2 class="problem-bridge__heading">
          {% capture __maybe %}{{ section.settings.heading | t }}{% endcapture %}
          {% if __maybe contains 'Translation missing' %}
            {{ section.settings.heading }}
          {% else %}
            {{ __maybe }}
          {% endif %}
        </h2>
      {% endif %}

      {% if section.settings.bridge_text != blank %}
        <div class="problem-bridge__text">
          {% capture __maybe %}{{ section.settings.bridge_text | t }}{% endcapture %}
          {% if __maybe contains 'Translation missing' %}
            {{ section.settings.bridge_text }}
          {% else %}
            {{ __maybe }}
          {% endif %}
        </div>
      {% endif %}

      {% if section.settings.highlight_text != blank %}
        <div class="problem-bridge__highlight">
          {% capture __maybe %}{{ section.settings.highlight_text | t }}{% endcapture %}
          {% if __maybe contains 'Translation missing' %}
            {{ section.settings.highlight_text }}
          {% else %}
            {{ __maybe }}
          {% endif %}
        </div>
      {% endif %}

      {% if section.blocks.size > 0 %}
        <ul class="problem-bridge__pain-points">
          {% for block in section.blocks %}
            {% if block.type == 'pain_point' %}
              <li class="problem-bridge__pain-point" {{ block.shopify_attributes }}>
                {% case block.settings.icon %}
                  {% when 'checkbox-empty' %}
                    <svg class="problem-bridge__pain-icon" aria-hidden="true" focusable="false" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <rect x="3" y="3" width="18" height="18" rx="2"/>
                    </svg>
                  {% when 'x-mark' %}
                    <svg class="problem-bridge__pain-icon" aria-hidden="true" focusable="false" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M18 6L6 18M6 6l12 12"/>
                    </svg>
                  {% when 'warning' %}
                    <svg class="problem-bridge__pain-icon" aria-hidden="true" focusable="false" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M12 9v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                {% endcase %}
                
                {% capture __maybe %}{{ block.settings.text | t }}{% endcapture %}
                {% if __maybe contains 'Translation missing' %}
                  <span>{{ block.settings.text }}</span>
                {% else %}
                  <span>{{ __maybe }}</span>
                {% endif %}
              </li>
            {% endif %}
          {% endfor %}
        </ul>
      {% endif %}
    </div>
  </div>
</div>

{{ 'section-problem-bridge.css' | asset_url | stylesheet_tag }}

{% schema %}
{
  "name": "Problem Bridge",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Sound Familiar?"
    },
    {
      "type": "textarea",
      "id": "bridge_text",
      "label": "Bridge Text",
      "default": "It's not your fault. Pills fail because 70% of nutrients never absorb. Your gut destroys them before they work."
    },
    {
      "type": "text",
      "id": "highlight_text",
      "label": "Highlight Text",
      "default": "There's a better way."
    },
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "Accent 1"
        },
        {
          "value": "accent-2",
          "label": "Accent 2"
        },
        {
          "value": "background-1",
          "label": "Background 1"
        },
        {
          "value": "background-2",
          "label": "Background 2"
        },
        {
          "value": "inverse",
          "label": "Inverse"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "background-1",
      "label": "Color scheme",
      "info": "Custom color scheme is edited at the bottom of section settings."
    },
    {
      "type": "select",
      "id": "text_alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "center",
      "label": "Text alignment"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    },
    {
      "type": "header",
      "content": "Custom color scheme"
    },
    {
      "type": "color",
      "id": "custom_colors_background",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color_background",
      "id": "custom_gradient_background",
      "label": "Background gradient"
    },
    {
      "type": "color",
      "id": "custom_colors_text",
      "label": "Text",
      "default": "#121212"
    }
  ],
  "blocks": [
    {
      "type": "pain_point",
      "name": "Pain Point",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            {
              "value": "checkbox-empty",
              "label": "Checkbox"
            },
            {
              "value": "x-mark",
              "label": "X Mark"
            },
            {
              "value": "warning",
              "label": "Warning"
            }
          ],
          "default": "checkbox-empty"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Pain point text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Problem Bridge",
      "blocks": [
        {
          "type": "pain_point",
          "settings": {
            "icon": "checkbox-empty",
            "text": "You wake up tired despite 7+ hours of sleep"
          }
        },
        {
          "type": "pain_point",
          "settings": {
            "icon": "checkbox-empty",
            "text": "Your energy crashes every afternoon"
          }
        },
        {
          "type": "pain_point",
          "settings": {
            "icon": "checkbox-empty",
            "text": "You've tried multiple supplements but can't stick to the routine"
          }
        },
        {
          "type": "pain_point",
          "settings": {
            "icon": "checkbox-empty",
            "text": "Caffeine gives you jitters, then a crash"
          }
        },
        {
          "type": "pain_point",
          "settings": {
            "icon": "checkbox-empty",
            "text": "You spend $200+/month on pills that don't deliver"
          }
        }
      ]
    }
  ]
}
{% endschema %}
