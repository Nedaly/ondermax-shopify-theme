<div class="announcement-bar">
  <span>{{ section.settings.message }}</span>
  <style>
    .announcement-bar{padding:8px 12px;text-align:center;font-weight:600}
  </style>
  {% schema %}
  {
    "name": "Announcement Bar",
    "settings": [
      { "id": "message", "type": "text", "label": "Message", "default": "Free worldwide shipping on SURGE + DRIFT bundle today" }
    ],
    "presets": [{ "name": "Announcement Bar" }]
  }
  {% endschema %}
</div>
